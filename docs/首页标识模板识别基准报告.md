# 首页标识模板识别基准报告

- 测试时间：2026-03-18 00:47:28
- 模板图：`data\images\home_indicator.png`
- 固定底图：`data\output\bench_home_indicator_screen.png`
- 阈值：`0.85`
- 当前桌面分辨率：`2560x1440`
- 实时抓屏命中状态：本次测量时当前桌面未出现首页标识，因此抓屏结果代表 miss-path；固定底图结果仍代表 hit-path。

## 结论

- 离线固定底图口径下，Rust `corrmatch` 平均 `match_ms_avg=23.199` ms，Python `pyscreeze` 平均 `match_ms_avg=111.252` ms，Rust 快 `x4.80`。
- 端到端抓屏口径下，Rust `corrmatch` 平均 `total_ms_avg=63.262` ms，Python `locateOnScreen` 平均 `total_ms_avg=150.304` ms，Rust 快 `x2.38`。
- 仅使用 `imageproc` 路线时，Rust 在固定底图下也快于 Python（`x3.47`），端到端抓屏口径也快于 Python（`x1.94`）；但 `corrmatch` 仍然是全场最佳。

## 识别链路确认

- Python 现网路径是 `src/super_buyer/services/screen_ops.py` 中的 `pyautogui.locateOnScreen(...)`。
- 本地 `pyscreeze` 源码显示：`locateOnScreen()` 每轮先 `screenshot(region=None)`，再调用 `locate()`；`_locateAll_opencv()` 里会把模板图和底图都转成 OpenCV 图像后执行 `cv2.matchTemplate(..., TM_CCOEFF_NORMED)`。
- Rust 侧直接复用了仓库已有的 `desktop/src-tauri/src/bin/template_bench.rs`、`desktop/src-tauri/src/automation/vision.rs` 与现成依赖 `corrmatch` / `imageproc`。

## 实测数据

| 场景 | 引擎 | 平均耗时(ms) | 最小(ms) | 最大(ms) | 备注 |
| --- | --- | ---: | ---: | ---: | --- |
| 固定底图 | Python `pyscreeze.locate` | 111.252 | 99.057 | 141.754 | 预加载底图，保留 PyScreeze 默认灰度策略 |
| 固定底图 | Python 直接 `cv2.matchTemplate` | 63.877 | 58.905 | 78.526 | 作为 Python/OpenCV 下限参考 |
| 固定底图 | Rust `imageproc` fast-image | 32.058 | 24.573 | 70.785 | 现有缓存模板 + 降采样粗定位 |
| 固定底图 | Rust `corrmatch` | 23.199 | 21.145 | 26.943 | 编译模板 + SIMD + rayon |
| 实时抓屏 | Python `screenshot` | 40.526 | 38.312 | 44.648 | 仅抓屏，不匹配 |
| 实时抓屏 | Python `locateOnScreen` | 150.304 | 143.991 | 158.155 | 生产路径基准 |
| 实时抓屏 | Rust `imageproc` fast-screen | 77.311 | 72.645 | 86.318 | GDI 抓屏 + 现有 fast probe |
| 实时抓屏 | Rust `corrmatch` | 63.262 | 56.605 | 76.489 | GDI 抓屏 + 编译模板匹配 |

## 关键分析

- Python 端到端平均约 `150 ms`，其中抓屏单项约 `41 ms`，剩余大头在 PyScreeze 每轮的灰度转换与 `cv2.matchTemplate` 全图滑窗。
- Rust `imageproc` 路线虽然也做了模板缓存和降采样粗定位，但在 `2560x1440` 全屏上仍要做细匹配，端到端平均 `total_ms_avg=77.311` ms，明显落后于 `corrmatch`。
- Rust `corrmatch` 路线把模板预编译成多尺度结构，启用了 `parallel=true`，并使用 crate 提供的 SIMD/rayon 特性，因此在固定底图和实时抓屏两种口径下都稳定拉开差距。
- 从当前结果看，若目标是替换现有首页标识识别实现，推荐优先把 Rust 侧的 `corrmatch` 路线产品化，而不是继续围绕 `imageproc` 做小修小补。

## 本轮选型取舍

- 已采用：`imageproc::template_matching`，因为仓库已有实现，可作为直接对标 OpenCV NCC 的 CPU 基线。
- 已采用：`corrmatch`，因为仓库已经依赖，且支持模板编译、并行和 SIMD，实测是当前最优解。
- 已调研未接入：`template-matching` crate。文档显示它基于 `wgpu` 做 GPU 加速；对当前 Windows RPA 工具来说，引入额外 GPU 运行时和部署约束，不如复用现有 `corrmatch` 划算。

## 复跑命令

```powershell
uv run python tools/bench_home_indicator.py
```

## 参考资料

- `imageproc` 模板匹配文档：<https://docs.rs/imageproc/latest/imageproc/template_matching/index.html>
- `template-matching` crate 文档：<https://docs.rs/template-matching/latest/template_matching/>
- `corrmatch` crate 文档：<https://docs.rs/corrmatch/latest/corrmatch/>
- 调研文章：<https://blog.csdn.net/HoKis/article/details/143697427>
